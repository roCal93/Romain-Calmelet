/**
 * Default export of the BackgroundHome component
 * Allows simplified import: import BackgroundHome from './BackgroundHome'
 * Re-exports the main component from the module
 */
export { default } from './BackgroundHome'

/**
 * Named export of the default configuration
 * Allows destructured import: import { DEFAULT_CONFIG } from './BackgroundHome'
 * Exposes configuration constants for external use
 */
export { DEFAULT_CONFIG } from './constants/config'
